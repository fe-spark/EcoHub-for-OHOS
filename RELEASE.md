### 更新说明 (v1.1.7)

- **播放器进度跳转**：点击与拖拽即时同步进度显示；`seekDone` 按落点匹配当前寻道，超时只解除锁定、不回写旧进度，避免 HLS / 缓冲期间进度回跳与卡死。
- **播放器画面比例**：全屏支持适应、填充、拉伸、16:9、4:3；按视频真实画幅缩放，消除黑框嵌套。
- **加载重试提示**：「点击重试」仅在打开或缓冲过慢时出现，寻道过程不展示，结束后立即消失。
- **首页推荐分类**：分类 Tag 改为名称首字书法字标，透明线框毛玻璃样式。
- **点播埋点补齐**：点击播放页「相关推荐」即时上报点播流转事件，并自动同步恢复本地历史播放进度与剧集。
