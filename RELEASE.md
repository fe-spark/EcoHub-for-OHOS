### 更新说明 (v1.1.6)

- **访问统计协议对齐**：客户端上报补齐 `source/page/app_version/device_model/device_id`，请求携带设备 ID；History / Tip / 自定义播放在页面出现时记 PV，不再在跳转前预上报。
- **首页推荐视觉**：轮播加高；片库分类改为微圆角毛玻璃，用名称末字作圆形字标，下拉刷新按色环均分重抽深色底，保证白字对比。
- **播放详情源栏**：横向 List 支持 `scrollToIndex`，补齐尾部空白与垂直居中，选中源能滚到视野内。
- **设备 ID**：首次生成改为 UUID，并用 in-flight 合并避免并发写出两个 ID。
