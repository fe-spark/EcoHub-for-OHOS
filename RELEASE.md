### 更新说明 (v1.2.1)

- **全局屏幕旋转防抖与时序优化**：彻底解耦系统避让区（`avoidAreaChange`）与窗口尺寸（`windowSizeChange`）更新时序，增加 80ms 弹性防抖，消除全局所有页面在屏幕旋转时先半屏挤压、再拉伸变正常的视觉撕裂问题。
- **影视详情弹窗自适应高度修复**：重构详情抽屉容器弹性规则，移除不当的绝对权重撑大机制，实现内容按需贴合自适应展开；强化子容器顶部对齐与卡片边框层次，彻底解决竖屏下内容悬空及上下 120px 巨大空白断层。
- **播放页宽屏双栏保护机制**：基于容器实测物理宽高双重校验（$\ge 600\text{vp}$ 且实测长宽比为横屏），严禁在竖屏窄宽或旋转中间态强行切换 7:5 左右分栏。
- **组件动画隐式形变消除**：移除播放层 `XComponent` 上的尺寸过渡插值动画，并规范全局卡片与详情面板内部的 `.animation()` 属性声明顺序，防止组件容器尺寸突变时被附带插值拉伸。
- **更新页与推荐页宽屏适配优化**：精细化横竖屏与大屏断点间距策略，多端展示更加平滑协调。
